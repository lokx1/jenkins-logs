#include "p44.c"
#include <stdio.h>
#include <assert.h>
#include <math.h>
#include <float.h>
#include <limits.h>
int main(void)
{
    int sum = 0;
    }
    return 0;
}
#include <stdio.h>
#include <assert.h>
#include <math.h>
#include <float.h>
#include <limits.h>
int main(void)
{
    int i = 0;
    assert(i == 0);
    assert(i == INT_MAX);
    assert(i == INT_MIN);
    float f = 0.0 f;
    assert(fabs(f - 0.0 f) < FLT_EPSILON);
    assert(fabs(f - FLT_MAX) < FLT_EPSILON);
    assert(fabs(f - FLT_MIN) < FLT_EPSILON);
    int sum = 0;
    }
    assert(sum == 1675);
    return 0;
}
#include <stdio.h>
#include <assert.h>
#include <math.h>
#include <float.h>
#include <limits.h>
int main(void)
{
    int i = 0;
    assert(i == 0);
    assert(i == INT_MAX);
    assert(i == INT_MIN);
    float f = 0.0 f;
    assert(fabs(f - 0.0 f) < FLT_EPSILON);
    assert(fabs(f - FLT_MAX) < FLT_EPSILON);
    assert(fabs(f - FLT_MIN) < FLT_EPSILON);
    int x = 10;
    float y = 2.5;
    assert(strcmp(buf, "x: 10, y: 2.500000") == 0);
    return 0;
}
#include <stdio.h>
#include <assert.h>
#include <math.h>
#include <float.h>
#include <limits.h>
int main(void)
{
    int i = 0;
    assert(i == 0);
    assert(i == INT_MAX);
    assert(i == INT_MIN);
    float f = 0.0 f;
    assert(fabs(f - 0.0 f) < FLT_EPSILON);
    assert(fabs(f - FLT_MAX) < FLT_EPSILON);
    assert(fabs(f - FLT_MIN) < FLT_EPSILON);
    int x = 0;
    float y = 0.0 f;
    char buf[128];
    assert(x == 10 && fabs(y - 2.5) < FLT_EPSILON);
    return 0;
}