#include "p33.c"
#include <stdio.h>
#include <assert.h>
#include <limits.h>
#include <float.h>
void bigONotation(int n)
{
        }
    }
}
void test_bigONotation()
{
    int n = 5;
    assert(1 == 1); 
    assert(1 == 1); 
    assert(1 == 1); 
    assert(0 == 1); 
}
int main(void)
{
    return 0;
}